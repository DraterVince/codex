using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayedCardChecker : MonoBehaviour
{
    [SerializeField] GameObject playedCard;

    private void Update()
    {
        //playedCard = transform.GetChild(0).gameObject;
    }
}